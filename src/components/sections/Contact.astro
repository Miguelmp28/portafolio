---
interface Props {
  contact: {
    title: string;
    description: string;
    address: string;
    mapUrl: string;
  };
}

const { contact } = Astro.props;
---

<section id="contacto" class="bg-stone-900 py-16 sm:py-20">
  <div class="mx-auto w-[min(1120px,92vw)]">

    <div
      class="rounded-2xl border border-stone-700 bg-stone-800 p-6 shadow-sm sm:p-8"
      data-reveal
      style="--reveal-from: translateY(32px) scale(0.98);"
    >
      <h2
        data-reveal
        style="--reveal-from: translateX(-24px); --reveal-delay: 80ms;"
        class="text-2xl font-bold tracking-tight text-white sm:text-3xl"
      >
        {contact.title}
      </h2>

      <p
        data-reveal
        style="--reveal-from: translateY(18px); --reveal-delay: 160ms;"
        class="mt-2 text-sm text-stone-400 sm:text-base"
      >
        {contact.description}
      </p>

      <p
        data-reveal
        style="--reveal-from: translateY(18px); --reveal-delay: 220ms;"
        class="mt-3 text-sm font-semibold text-stone-300 sm:text-base"
      >
        {contact.address}
      </p>

      <div
        data-reveal
        style="--reveal-from: translateY(16px) scale(0.95); --reveal-delay: 300ms; --reveal-dur: 550ms;"
      >
        <a
          href={contact.mapUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="mt-5 inline-flex rounded-full border border-stone-600 bg-white px-5 py-2.5 text-sm font-semibold text-stone-900 shadow-sm transition-colors duration-300 hover:bg-rose-500 hover:text-white hover:border-rose-500"
        >
          Ver en Google Maps
        </a>
      </div>
    </div>

  </div>
</section>
